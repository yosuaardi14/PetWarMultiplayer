import 'package:flutter/material.dart';

class HostPanel extends StatelessWidget {
  const HostPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
